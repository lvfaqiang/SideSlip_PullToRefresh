<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin">

    <TextView
        android:id="@+id/tv_listview"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_marginTop="20dp"
        android:background="#cccccc"
        android:gravity="center"
        android:text="pulltorefresh_ListView"
        android:textColor="#000" />

    <TextView
        android:id="@+id/tv_recyclerView"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_marginTop="20dp"
        android:background="#cccccc"
        android:gravity="center"
        android:text="pulltorefresh_RecyclerView"
        android:textColor="#000" />

    <TextView
        android:id="@+id/tv_swipeRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_marginTop="20dp"
        android:background="#cccccc"
        android:gravity="center"
        android:text="pulltorefresh_Swipe_RecyclerView"
        android:textColor="#000" />

</LinearLayout>
